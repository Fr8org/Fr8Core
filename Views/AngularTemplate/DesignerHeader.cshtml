<div class="designer-header">
    <div class="process-template-name">{{processTemplate.name}}</div>
    <div class="btn-group">
        <label class="btn switch-btn" ng-model="processTemplate.routeState" btn-radio="1" ng-change="onStateChange()">Inactive</label>
        <label class="btn switch-btn" ng-model="processTemplate.routeState" btn-radio="2" ng-change="onStateChange()">Active</label>
    </div>
</div>
