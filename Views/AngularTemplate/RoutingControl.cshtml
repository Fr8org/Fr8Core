<div class='form-group'>
    <table >
        <tr><td><label class='control-label'>If the result is {{plan.measurementValue}}</label>
            <!--</td></tr>
        <tr>
            <td>-->
                <div class="radio">
                    <label>
                        <input type="radio" name="routing_rc_{{uniqueDirectiveId}}" value="none" ng-model="plan.selection" />
                        <span>Go to Next Activity</span>
                    </label>
                </div>
                <!--</td>
                    </tr>
                    <tr>
                        <td>-->
                <div class="radio">
                    <label>
                        <input type="radio" name="routing_rc_{{uniqueDirectiveId}}" value="stop" ng-model="plan.selection" />
                        <span>Stop</span>
                    </label>
                </div>
            <!--</td>
        </tr>
        <tr>
            <td>-->
                <div class="radio">
                    <label>
                        <input type="radio" name="routing_rc_{{uniqueDirectiveId}}" value="return" ng-model="plan.selection" />
                        <span>Return to Previous Activity:</span>
                    </label>
                    <div class="previous-action-selection">
                        <select name="repeatSelect_rc_{{uniqueDirectiveId}}" ng-model="plan.previousActionList.selectionId" ng-disabled="plan.selection != 'return'">
                            <option ng-repeat="option in plan.previousActionList.choices" value="{{option.Id}}">{{option.Label}}</option>
                        </select>
                    </div>
                </div>
            <!--</td>
        </tr>
        <tr>
            <td>-->
                <div class="radio">
                    <label>
                        <input type="radio" name="routing_rc_{{uniqueDirectiveId}}" value="jump" ng-model="plan.selection" />
                        <span>Jump to a Different Process Node</span>
                    </label>
                    <div class="process-node-selection">
                        <!--TODO: To be updated when finalized-->
                        <!--<select name="repeatSelect" ng-model="plan.available_process_node.selection_id" ng-disabled="routing.Selection != jump">
                                <option ng-repeat="option in plan.available_process_node.choices" value="{{option.Id}}">{{option.Label}}</option>
                            </select>-->
                    </div>
                </div>
            </td>
        </tr>
        <tr>
            <td>
                FOR DEMO ONLY:
                <br />
                selection: {{plan.selection}}
                <br />
                plan: {{plan.previousActionList.selectionId}}
            </td>
        </tr>
    </table>
</div>