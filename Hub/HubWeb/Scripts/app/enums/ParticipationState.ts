module dockyard.enums {
    // If you add, change or remove items, please do the same in ParticipationState.
    export enum ParticipationState {
        Unapproved = 0,
        Approved = 1,
        Blocked = 2,
        Deleted = 3
    };
}
